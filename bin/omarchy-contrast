#!/bin/bash
# Toggle the Omarchy contrast checker overlay.
# Usage: omarchy-contrast [fg-hex] [bg-hex]
payload="{}"
if [[ -n $1 || -n $2 ]]; then
  payload=$(printf '{"fg":"%s","bg":"%s"}' "${1:-}" "${2:-}")
fi
exec omarchy-shell shell toggle loamstudios.contrast "$payload"
